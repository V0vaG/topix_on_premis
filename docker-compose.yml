services:
    app:
        build: ./app
        volumes:
            - /home/vova/topix:/root/script_files/topix
    nginx:
        container_name: nginx
        restart: always
        build: ./nginx
        depends_on:
            - app
        ports:
            - "80:80"
